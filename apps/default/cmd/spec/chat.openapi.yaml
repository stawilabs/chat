openapi: 3.1.0
info:
  title: Chat Service
  description: The Chat Service provides endpoints for real-time, secure messaging between users and devices. It supports sending, receiving, and synchronizing messages across rooms, direct chats, and group conversations, with optional end-to-end encryption. The service is designed for mobile, desktop, and web clients, supporting both streaming and standard request-response operations. APIs are consistent, well-structured, and optimized for low-latency delivery, even on limited network connections.
  contact:
    name: Ant Investor Ltd
    url: https://github.com/antinvestor/service-chat
    email: info@antinvestor.com
  license:
    name: Apache License
    url: https://github.com/antinvestor/apis/blob/master/LICENSE
  version: v1.0.0
x-service-namespace: service_chat
x-service-permissions:
  - event_view
  - event_send
  - room_view
  - room_create
  - room_update
  - room_delete
  - subscription_view
  - subscription_manage
  - proposal_view
  - proposal_manage
paths:
  /chat.v1.ChatService/AddRoomSubscriptions:
    post:
      tags:
        - Subscriptions
        - chat.v1.ChatService
      summary: Add members to a room
      description: Adds one or more users to a chat room with specified roles. The requesting user must have owner or moderator privileges in the room.
      operationId: addRoomSubscriptions
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.AddRoomSubscriptionsRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.AddRoomSubscriptionsResponse'
      x-required-permissions:
        - subscription_manage
  /chat.v1.ChatService/CreateRoom:
    post:
      tags:
        - Rooms
        - chat.v1.ChatService
      summary: Create a new chat room
      description: Creates a new chat room with specified configuration. The creator is automatically added as a member with owner privileges. Supports both public and private rooms.
      operationId: createRoom
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.CreateRoomRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.CreateRoomResponse'
      x-required-permissions:
        - room_create
  /chat.v1.ChatService/DeleteRoom:
    post:
      tags:
        - Rooms
        - chat.v1.ChatService
      summary: Delete a chat room
      description: Permanently deletes a chat room and all its messages. This action cannot be undone. Only room owners can delete rooms.
      operationId: deleteRoom
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.DeleteRoomRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.DeleteRoomResponse'
      x-required-permissions:
        - room_delete
  /chat.v1.ChatService/GetEvent:
    get:
      tags:
        - Messages
        - chat.v1.ChatService
      summary: Retrieve a single event by ID
      description: Fetches a single event by its unique identifier. Useful for deep-linking to messages, rendering reply threads, and verifying event existence.
      operationId: getEvent
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
        - name: message
          in: query
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.GetEventRequest'
        - name: encoding
          in: query
          required: true
          schema:
            $ref: '#/components/schemas/encoding'
        - name: base64
          in: query
          schema:
            $ref: '#/components/schemas/base64'
        - name: compression
          in: query
          schema:
            $ref: '#/components/schemas/compression'
        - name: connect
          in: query
          schema:
            $ref: '#/components/schemas/connect'
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.GetEventResponse'
      x-required-permissions:
        - event_view
    post:
      tags:
        - Messages
        - chat.v1.ChatService
      summary: Retrieve a single event by ID
      description: Fetches a single event by its unique identifier. Useful for deep-linking to messages, rendering reply threads, and verifying event existence.
      operationId: getEvent
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.GetEventRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.GetEventResponse'
      x-required-permissions:
        - event_view
  /chat.v1.ChatService/GetHistory:
    get:
      tags:
        - Messages
        - chat.v1.ChatService
      summary: Retrieve message history for a room
      description: Fetches paginated message history for a specified room using cursor-based navigation. Supports forward and backward pagination.
      operationId: getHistory
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
        - name: message
          in: query
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.GetHistoryRequest'
        - name: encoding
          in: query
          required: true
          schema:
            $ref: '#/components/schemas/encoding'
        - name: base64
          in: query
          schema:
            $ref: '#/components/schemas/base64'
        - name: compression
          in: query
          schema:
            $ref: '#/components/schemas/compression'
        - name: connect
          in: query
          schema:
            $ref: '#/components/schemas/connect'
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.GetHistoryResponse'
      x-required-permissions:
        - event_view
    post:
      tags:
        - Messages
        - chat.v1.ChatService
      summary: Retrieve message history for a room
      description: Fetches paginated message history for a specified room using cursor-based navigation. Supports forward and backward pagination.
      operationId: getHistory
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.GetHistoryRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.GetHistoryResponse'
      x-required-permissions:
        - event_view
  /chat.v1.ChatService/GetRoom:
    get:
      tags:
        - Rooms
        - chat.v1.ChatService
      summary: Retrieve a single room by ID
      description: Fetches a single chat room by its unique identifier. Returns room details including name, description, type, and metadata.
      operationId: getRoom
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
        - name: message
          in: query
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.GetRoomRequest'
        - name: encoding
          in: query
          required: true
          schema:
            $ref: '#/components/schemas/encoding'
        - name: base64
          in: query
          schema:
            $ref: '#/components/schemas/base64'
        - name: compression
          in: query
          schema:
            $ref: '#/components/schemas/compression'
        - name: connect
          in: query
          schema:
            $ref: '#/components/schemas/connect'
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.GetRoomResponse'
      x-required-permissions:
        - room_view
    post:
      tags:
        - Rooms
        - chat.v1.ChatService
      summary: Retrieve a single room by ID
      description: Fetches a single chat room by its unique identifier. Returns room details including name, description, type, and metadata.
      operationId: getRoom
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.GetRoomRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.GetRoomResponse'
      x-required-permissions:
        - room_view
  /chat.v1.ChatService/GetSubscriptionSettings:
    get:
      tags:
        - Subscriptions
        - chat.v1.ChatService
      summary: Get subscription settings for a room
      description: Retrieves per-room notification and display settings for the requesting user's subscription.
      operationId: getSubscriptionSettings
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
        - name: message
          in: query
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.GetSubscriptionSettingsRequest'
        - name: encoding
          in: query
          required: true
          schema:
            $ref: '#/components/schemas/encoding'
        - name: base64
          in: query
          schema:
            $ref: '#/components/schemas/base64'
        - name: compression
          in: query
          schema:
            $ref: '#/components/schemas/compression'
        - name: connect
          in: query
          schema:
            $ref: '#/components/schemas/connect'
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.GetSubscriptionSettingsResponse'
      x-required-permissions:
        - subscription_view
    post:
      tags:
        - Subscriptions
        - chat.v1.ChatService
      summary: Get subscription settings for a room
      description: Retrieves per-room notification and display settings for the requesting user's subscription.
      operationId: getSubscriptionSettings
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.GetSubscriptionSettingsRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.GetSubscriptionSettingsResponse'
      x-required-permissions:
        - subscription_view
  /chat.v1.ChatService/ListProposals:
    get:
      tags:
        - Proposals
        - chat.v1.ChatService
      summary: List pending proposals for a room
      description: Retrieves all pending proposals for a room that require approval. Only room members can view proposals.
      operationId: listProposals
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
        - name: message
          in: query
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.ListProposalsRequest'
        - name: encoding
          in: query
          required: true
          schema:
            $ref: '#/components/schemas/encoding'
        - name: base64
          in: query
          schema:
            $ref: '#/components/schemas/base64'
        - name: compression
          in: query
          schema:
            $ref: '#/components/schemas/compression'
        - name: connect
          in: query
          schema:
            $ref: '#/components/schemas/connect'
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.ListProposalsResponse'
      x-required-permissions:
        - proposal_view
    post:
      tags:
        - Proposals
        - chat.v1.ChatService
      summary: List pending proposals for a room
      description: Retrieves all pending proposals for a room that require approval. Only room members can view proposals.
      operationId: listProposals
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.ListProposalsRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.ListProposalsResponse'
      x-required-permissions:
        - proposal_view
  /chat.v1.ChatService/Live:
    post:
      tags:
        - Real-time
        - chat.v1.ChatService
      summary: Executes realtime connection updates
      description: Executes realtime updates for events from a user and optionally Broadcasts to affiliated active participants.
      operationId: Live
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.LiveRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.LiveResponse'
      x-required-permissions:
        - event_view
  /chat.v1.ChatService/RemoveRoomSubscriptions:
    post:
      tags:
        - Subscriptions
        - chat.v1.ChatService
      summary: Remove members from a room
      description: Removes one or more users from a chat room. The requesting user must have owner or moderator privileges in the room, unless removing themselves.
      operationId: removeRoomSubscriptions
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.RemoveRoomSubscriptionsRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.RemoveRoomSubscriptionsResponse'
      x-required-permissions:
        - subscription_manage
  /chat.v1.ChatService/SearchRoomSubscriptions:
    get:
      tags:
        - Subscriptions
        - chat.v1.ChatService
      summary: List room members
      description: Retrieves a paginated list of users subscribed to a room, along with their roles and activity information.
      operationId: searchRoomSubscriptions
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
        - name: message
          in: query
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.SearchRoomSubscriptionsRequest'
        - name: encoding
          in: query
          required: true
          schema:
            $ref: '#/components/schemas/encoding'
        - name: base64
          in: query
          schema:
            $ref: '#/components/schemas/base64'
        - name: compression
          in: query
          schema:
            $ref: '#/components/schemas/compression'
        - name: connect
          in: query
          schema:
            $ref: '#/components/schemas/connect'
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.SearchRoomSubscriptionsResponse'
      x-required-permissions:
        - subscription_view
    post:
      tags:
        - Subscriptions
        - chat.v1.ChatService
      summary: List room members
      description: Retrieves a paginated list of users subscribed to a room, along with their roles and activity information.
      operationId: searchRoomSubscriptions
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.SearchRoomSubscriptionsRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.SearchRoomSubscriptionsResponse'
      x-required-permissions:
        - subscription_view
  /chat.v1.ChatService/SearchRooms: {}
  /chat.v1.ChatService/SendEvent:
    post:
      tags:
        - Messages
        - chat.v1.ChatService
      summary: Send an event to a room
      description: Sends one or more events to chat rooms. Supports text, attachments, reactions, and system messages. Idempotent when idempotency_key header is provided.
      operationId: sendEvent
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.SendEventRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.SendEventResponse'
      x-required-permissions:
        - event_send
  /chat.v1.ChatService/SubmitProposal:
    post:
      tags:
        - Proposals
        - chat.v1.ChatService
      summary: Submit a decision on a pending proposal
      description: Approves or rejects a pending proposal. If approved, the proposed change is executed. Only room owners can submit proposal decisions.
      operationId: submitProposal
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.SubmitProposalRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.SubmitProposalResponse'
      x-required-permissions:
        - proposal_manage
  /chat.v1.ChatService/UpdateRoom:
    post:
      tags:
        - Rooms
        - chat.v1.ChatService
      summary: Update a chat room
      description: Updates the configuration of an existing chat room including name, topic, and metadata. Only room owners and moderators can update room settings.
      operationId: updateRoom
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.UpdateRoomRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.UpdateRoomResponse'
      x-required-permissions:
        - room_update
  /chat.v1.ChatService/UpdateSubscriptionRole:
    post:
      tags:
        - Subscriptions
        - chat.v1.ChatService
      summary: Update a member's role in a room
      description: Updates the role(s) of a user in a chat room. The requesting user must have owner or moderator privileges in the room.
      operationId: updateSubscriptionRole
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.UpdateSubscriptionRoleRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.UpdateSubscriptionRoleResponse'
      x-required-permissions:
        - subscription_manage
  /chat.v1.ChatService/UpdateSubscriptionSettings:
    post:
      tags:
        - Subscriptions
        - chat.v1.ChatService
      summary: Update subscription settings for a room
      description: Updates per-room notification and display settings for the requesting user's subscription. Supports muting, archiving, and notification level changes.
      operationId: updateSubscriptionSettings
      parameters:
        - name: Connect-Protocol-Version
          in: header
          required: true
          schema:
            $ref: '#/components/schemas/connect-protocol-version'
        - name: Connect-Timeout-Ms
          in: header
          schema:
            $ref: '#/components/schemas/connect-timeout-header'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/chat.v1.UpdateSubscriptionSettingsRequest'
        required: true
      responses:
        default:
          description: Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connect.error'
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chat.v1.UpdateSubscriptionSettingsResponse'
      x-required-permissions:
        - subscription_manage
components:
  schemas:
    base64:
      type: boolean
      title: base64
      description: Specifies if the message query param is base64 encoded, which may be required for binary data
    chat.v1.AckEvent:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        eventId:
          type: array
          items:
            type: string
            maxLength: 40
            minLength: 3
            pattern: '[0-9a-z_-]{3,40}'
          title: event_id
          minItems: 1
        subscriptionId:
          type: string
          title: subscription_id
        ackAt:
          title: ack_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        error:
          oneOf:
            - $ref: '#/components/schemas/common.v1.ErrorDetail'
            - type: "null"
          title: error
          description: if set, indicates failure reason for this event
      title: AckEvent
      additionalProperties: false
      description: |-
        Acknowledgement for event(s) received; server uses it to free ephemeral delivery buffers.
         ack_event_id: last event_id client processed (inclusive).
         If error is set, indicates the event failed to send/process correctly.
    chat.v1.AddRoomSubscriptionsRequest:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        members:
          type: array
          items:
            $ref: '#/components/schemas/chat.v1.RoomSubscription'
          title: members
          description: include roles if desired
      title: AddRoomSubscriptionsRequest
      additionalProperties: false
    chat.v1.AddRoomSubscriptionsResponse:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        error:
          title: error
          $ref: '#/components/schemas/common.v1.ErrorDetail'
      title: AddRoomSubscriptionsResponse
      additionalProperties: false
    chat.v1.AttachmentContent:
      type: object
      properties:
        attachmentId:
          type: string
          title: attachment_id
          description: Logical identifier of the attachment
        filename:
          type: string
          title: filename
          description: Original filename (optional, user-facing)
        mimeType:
          type: string
          title: mime_type
          description: MIME type (e.g. image/png, application/pdf)
        sizeBytes:
          type:
            - integer
            - string
          title: size_bytes
          format: int64
          description: Size in bytes (for quota enforcement & UX)
        uri:
          type: string
          title: uri
          description: Content location (signed URL or opaque locator)
        previews:
          type: array
          items:
            $ref: '#/components/schemas/chat.v1.AttachmentPreview'
          title: previews
          description: Optional previews / thumbnails
        caption:
          oneOf:
            - $ref: '#/components/schemas/chat.v1.TextContent'
            - type: "null"
          title: caption
          description: Optional caption text
        encrypted:
          type: boolean
          title: encrypted
          description: Indicates whether attachment is end-to-end encrypted
        checksum:
          type:
            - string
            - "null"
          title: checksum
          description: Optional content hash (e.g. sha256:base64)
      title: AttachmentContent
      additionalProperties: false
    chat.v1.AttachmentPreview:
      type: object
      properties:
        mimeType:
          type: string
          title: mime_type
        width:
          type: integer
          title: width
          format: int32
        height:
          type: integer
          title: height
          format: int32
        uri:
          type: string
          title: uri
        sizeBytes:
          type:
            - integer
            - string
          title: size_bytes
          format: int64
      title: AttachmentPreview
      additionalProperties: false
    chat.v1.CallContent:
      type: object
      properties:
        callId:
          type: string
          title: call_id
        type:
          title: type
          $ref: '#/components/schemas/chat.v1.CallContent.CallType'
        action:
          title: action
          description: |-
            OFFER, ANSWER → sdp required
             ICE_CANDIDATE → ice_candidate required
             END → no payload required
          $ref: '#/components/schemas/chat.v1.CallContent.CallAction'
        sdp:
          type:
            - string
            - "null"
          title: sdp
          description: WebRTC payload (SDP or ICE candidate)
        iceCandidate:
          type:
            - string
            - "null"
          title: ice_candidate
          description: Optional ICE candidate (when action = ICE_CANDIDATE)
        metadata:
          title: metadata
          description: Optional metadata (bitrate, codecs, device hints)
          $ref: '#/components/schemas/google.protobuf.Struct'
      title: CallContent
      additionalProperties: false
    chat.v1.CallContent.CallAction:
      type: string
      title: CallAction
      enum:
        - CALL_ACTION_UNSPECIFIED
        - CALL_ACTION_OFFER
        - CALL_ACTION_ANSWER
        - CALL_ACTION_ICE_CANDIDATE
        - CALL_ACTION_END
    chat.v1.CallContent.CallType:
      type: string
      title: CallType
      enum:
        - CALL_TYPE_UNSPECIFIED
        - CALL_TYPE_AUDIO
        - CALL_TYPE_VIDEO
        - CALL_TYPE_SCREEN_SHARE
    chat.v1.ClientCommand:
      type: object
      oneOf:
        - type: object
          properties:
            ack:
              title: ack
              $ref: '#/components/schemas/chat.v1.AckEvent'
          title: ack
          required:
            - ack
        - type: object
          properties:
            event:
              title: event
              $ref: '#/components/schemas/chat.v1.RoomEvent'
          title: event
          required:
            - event
        - type: object
          properties:
            presence:
              title: presence
              description: send a presence message to a room
              $ref: '#/components/schemas/chat.v1.PresenceEvent'
          title: presence
          required:
            - presence
        - type: object
          properties:
            readMarker:
              title: read_marker
              $ref: '#/components/schemas/chat.v1.ReadMarker'
          title: read_marker
          required:
            - readMarker
        - type: object
          properties:
            receipt:
              title: receipt
              description: receipt acknowledgement for message delivery
              $ref: '#/components/schemas/chat.v1.ReceiptEvent'
          title: receipt
          required:
            - receipt
        - type: object
          properties:
            typing:
              title: typing
              $ref: '#/components/schemas/chat.v1.TypingEvent'
          title: typing
          required:
            - typing
      title: ClientCommand
      additionalProperties: false
      description: |-
        ClientCommand represents durable state changes initiated by the client.
         Commands are validated, persisted, and broadcast as needed.
         ClientCommand results in at least one RoomEvent being emitted.
    chat.v1.CreateRoomRequest:
      type: object
      properties:
        id:
          type: string
          title: id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
          description: optional client-provided idempotency key
        name:
          type: string
          title: name
          maxLength: 200
          minLength: 2
        description:
          type: string
          title: description
        type:
          title: type
          $ref: '#/components/schemas/chat.v1.RoomType'
        isPrivate:
          type: boolean
          title: is_private
        members:
          type: array
          items:
            $ref: '#/components/schemas/common.v1.ContactLink'
          title: members
          description: optional
        metadata:
          title: metadata
          $ref: '#/components/schemas/google.protobuf.Struct'
        requiresApproval:
          type: boolean
          title: requires_approval
          description: If true, changes to this room require approval from owners before execution
      title: CreateRoomRequest
      additionalProperties: false
    chat.v1.CreateRoomResponse:
      type: object
      properties:
        room:
          title: room
          $ref: '#/components/schemas/chat.v1.Room'
        error:
          title: error
          $ref: '#/components/schemas/common.v1.ErrorDetail'
      title: CreateRoomResponse
      additionalProperties: false
    chat.v1.DeleteRoomRequest:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
      title: DeleteRoomRequest
      additionalProperties: false
    chat.v1.DeleteRoomResponse:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        error:
          title: error
          $ref: '#/components/schemas/common.v1.ErrorDetail'
      title: DeleteRoomResponse
      additionalProperties: false
    chat.v1.EncryptedContent:
      type: object
      properties:
        algorithm:
          type: string
          title: algorithm
          description: |-
            Encryption scheme identifier
             Examples: "olm.v2", "megolm.v1", "x25519-aesgcm"
        ciphertext:
          type: string
          title: ciphertext
          format: byte
          description: Base64 or binary-safe encoded ciphertext
        nonce:
          type:
            - string
            - "null"
          title: nonce
          format: byte
          description: Optional per-message nonce / IV
        senderKeyId:
          type:
            - string
            - "null"
          title: sender_key_id
          description: Optional sender key identifier
        recipientKeyIds:
          type: array
          items:
            type: string
          title: recipient_key_ids
          description: Optional recipient key references (for fan-out)
        aad:
          type:
            - string
            - "null"
          title: aad
          format: byte
          description: Additional authenticated data (AAD)
        sessionId:
          type:
            - string
            - "null"
          title: session_id
          description: Optional key agreement context identifier
      title: EncryptedContent
      additionalProperties: false
    chat.v1.GetEventRequest:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        eventId:
          type: string
          title: event_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
      title: GetEventRequest
      additionalProperties: false
    chat.v1.GetEventResponse:
      type: object
      properties:
        event:
          title: event
          $ref: '#/components/schemas/chat.v1.RoomEvent'
        error:
          title: error
          $ref: '#/components/schemas/common.v1.ErrorDetail'
      title: GetEventResponse
      additionalProperties: false
    chat.v1.GetHistoryRequest:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
        cursor:
          title: cursor
          $ref: '#/components/schemas/common.v1.PageCursor'
        forward:
          type: boolean
          title: forward
          description: 'direction: FORWARD means older -> newer; BACKWARD newer -> older (default BACKWARD).'
      title: GetHistoryRequest
      additionalProperties: false
      description: |-
        History request: paging via opaque cursor. 'limit' is capped by server (e.g. 100).
         Ordering is determined by XID lexicographic order.
         Cursor encapsulates last seen event ID.
    chat.v1.GetHistoryResponse:
      type: object
      properties:
        events:
          type: array
          items:
            $ref: '#/components/schemas/chat.v1.RoomEvent'
          title: events
        nextCursor:
          type: string
          title: next_cursor
          description: opaque; empty when no more pages
        prevCursor:
          type: string
          title: prev_cursor
      title: GetHistoryResponse
      additionalProperties: false
    chat.v1.GetRoomRequest:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
      title: GetRoomRequest
      additionalProperties: false
    chat.v1.GetRoomResponse:
      type: object
      properties:
        room:
          title: room
          $ref: '#/components/schemas/chat.v1.Room'
        error:
          title: error
          $ref: '#/components/schemas/common.v1.ErrorDetail'
      title: GetRoomResponse
      additionalProperties: false
    chat.v1.GetSubscriptionSettingsRequest:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        subscriptionId:
          type: string
          title: subscription_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
      title: GetSubscriptionSettingsRequest
      additionalProperties: false
    chat.v1.GetSubscriptionSettingsResponse:
      type: object
      properties:
        settings:
          title: settings
          $ref: '#/components/schemas/chat.v1.SubscriptionSettings'
        error:
          title: error
          $ref: '#/components/schemas/common.v1.ErrorDetail'
      title: GetSubscriptionSettingsResponse
      additionalProperties: false
    chat.v1.ListProposalsRequest:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        state:
          oneOf:
            - $ref: '#/components/schemas/chat.v1.ProposalState'
            - type: "null"
          title: state
          description: 'Optional: filter by state (default: only PENDING)'
        cursor:
          title: cursor
          $ref: '#/components/schemas/common.v1.PageCursor'
      title: ListProposalsRequest
      additionalProperties: false
    chat.v1.ListProposalsResponse:
      type: object
      properties:
        proposals:
          type: array
          items:
            $ref: '#/components/schemas/chat.v1.Proposal'
          title: proposals
        nextCursor:
          type: string
          title: next_cursor
        prevCursor:
          type: string
          title: prev_cursor
      title: ListProposalsResponse
      additionalProperties: false
    chat.v1.LiveRequest:
      type: object
      properties:
        source:
          title: source
          $ref: '#/components/schemas/common.v1.ContactLink'
        clientStates:
          type: array
          items:
            $ref: '#/components/schemas/chat.v1.ClientCommand'
          title: client_states
      title: LiveRequest
      additionalProperties: false
    chat.v1.LiveResponse:
      type: object
      properties:
        error:
          title: error
          $ref: '#/components/schemas/common.v1.ErrorDetail'
      title: LiveResponse
      additionalProperties: false
    chat.v1.ModerationContent:
      type: object
      properties:
        body:
          type: string
          title: body
          maxLength: 10000
          minLength: 1
          description: |-
            Human-readable message body describing the action.
             Intended for display to end users.
        actorSubscriptionId:
          type: string
          title: actor_subscription_id
          maxLength: 32
          minLength: 1
          description: Subscription ID of the actor who initiated the action.
        targetSubscriptionIds:
          type: array
          items:
            type: string
            maxLength: 40
            minLength: 3
            pattern: '[0-9a-z_-]{3,40}'
          title: target_subscription_ids
          uniqueItems: true
          description: |-
            Subscription IDs of entities targeted by the action.
             Must not contain duplicates and must not include the actor.
        metadata:
          title: metadata
          description: |-
            Optional structured metadata associated with the action.
             Use only for non-indexed, auxiliary data.
          $ref: '#/components/schemas/google.protobuf.Struct'
        language:
          type:
            - string
            - "null"
          title: language
          pattern: ^[a-zA-Z]{2,3}(-[a-zA-Z0-9]{2,8})*$
          description: Optional language tag for the body, using BCP-47 (e.g. "en", "fr-CA").
      title: ModerationContent
      additionalProperties: false
    chat.v1.MotionContent:
      type: object
      properties:
        id:
          type: string
          title: id
        title:
          type: string
          title: title
          description: Human-readable description
        description:
          type: string
          title: description
        eligibleRoles:
          type: array
          items:
            type: string
          title: eligible_roles
          description: Roles allowed to vote; eligible_votes is derived
        eligibleVotes:
          type:
            - integer
            - "null"
          title: eligible_votes
          description: Snapshot of eligible voters at creation time (optional, cached)
        passingRule:
          title: passing_rule
          description: |-
            Number of votes required to pass
             Can represent absolute count or percentage thresholds
             Passing rule
          $ref: '#/components/schemas/chat.v1.PassingRule'
        choices:
          type: array
          items:
            $ref: '#/components/schemas/chat.v1.VoteChoice'
          title: choices
          description: Available choices (YES / NO / ABSTAIN, etc)
        closesAt:
          type:
            - integer
            - string
            - "null"
          title: closes_at
          format: int64
          description: Optional deadline (unix seconds)
      title: MotionContent
      additionalProperties: false
    chat.v1.MotionTally:
      type: object
      properties:
        motionId:
          type: string
          title: motion_id
        eligibleVotes:
          type: integer
          title: eligible_votes
          description: Total eligible voters
        tallies:
          type: array
          items:
            $ref: '#/components/schemas/chat.v1.VoteTally'
          title: tallies
          description: Votes per choice
        totalVotesCast:
          type: integer
          title: total_votes_cast
          description: Derived counts
        deadVotes:
          type: integer
          title: dead_votes
          description: Dead votes (invalid, expired, disqualified)
        targetVotes:
          type: integer
          title: target_votes
          description: Passing evaluation
        passed:
          type: boolean
          title: passed
        closed:
          type: boolean
          title: closed
      title: MotionTally
      additionalProperties: false
    chat.v1.NotificationLevel:
      type: string
      title: NotificationLevel
      enum:
        - NOTIFICATION_LEVEL_UNSPECIFIED
        - NOTIFICATION_LEVEL_ALL
        - NOTIFICATION_LEVEL_MENTIONS
        - NOTIFICATION_LEVEL_NONE
      description: NotificationLevel controls how notifications are delivered for a room subscription.
    chat.v1.PassingRule:
      type: object
      allOf:
        - properties:
            passingChoiceId:
              type: string
              title: passing_choice_id
              description: Which choice constitutes "passing" (usually YES)
        - oneOf:
            - type: object
              properties:
                absolute:
                  type: integer
                  title: absolute
                  description: Absolute number of votes required
              title: absolute
              required:
                - absolute
            - type: object
              properties:
                percentage:
                  type: integer
                  title: percentage
                  description: Percentage required to pass (0–100)
              title: percentage
              required:
                - percentage
      title: PassingRule
      additionalProperties: false
    chat.v1.Payload:
      type: object
      allOf:
        - properties:
            type:
              title: type
              description: |-
                The `type` MUST correspond to the populated payload.
                 Servers MUST reject mismatches.
              $ref: '#/components/schemas/chat.v1.PayloadType'
        - oneOf:
            - type: object
              properties:
                attachment:
                  title: attachment
                  description: type = PAYLOAD_TYPE_ATTACHMENT
                  $ref: '#/components/schemas/chat.v1.AttachmentContent'
              title: attachment
              required:
                - attachment
            - type: object
              properties:
                call:
                  title: call
                  description: type = PAYLOAD_TYPE_CALL
                  $ref: '#/components/schemas/chat.v1.CallContent'
              title: call
              required:
                - call
            - type: object
              properties:
                default:
                  title: default
                  description: type = PAYLOAD_TYPE_UNSPECIFIED
                  $ref: '#/components/schemas/google.protobuf.Struct'
              title: default
              required:
                - default
            - type: object
              properties:
                encrypted:
                  title: encrypted
                  description: type = PAYLOAD_TYPE_ENCRYPTED
                  $ref: '#/components/schemas/chat.v1.EncryptedContent'
              title: encrypted
              required:
                - encrypted
            - type: object
              properties:
                moderation:
                  title: moderation
                  description: type = PAYLOAD_TYPE_MODERATION
                  $ref: '#/components/schemas/chat.v1.ModerationContent'
              title: moderation
              required:
                - moderation
            - type: object
              properties:
                motion:
                  title: motion
                  description: type = PAYLOAD_TYPE_MOTION
                  $ref: '#/components/schemas/chat.v1.MotionContent'
              title: motion
              required:
                - motion
            - type: object
              properties:
                motionTally:
                  title: motion_tally
                  description: type = PAYLOAD_TYPE_MOTION_TALLY
                  $ref: '#/components/schemas/chat.v1.MotionTally'
              title: motion_tally
              required:
                - motionTally
            - type: object
              properties:
                reaction:
                  title: reaction
                  description: type = PAYLOAD_TYPE_REACTION
                  $ref: '#/components/schemas/chat.v1.ReactionContent'
              title: reaction
              required:
                - reaction
            - type: object
              properties:
                roomChange:
                  title: room_change
                  description: type = PAYLOAD_TYPE_ROOM_CHANGE
                  $ref: '#/components/schemas/chat.v1.RoomChangeContent'
              title: room_change
              required:
                - roomChange
            - type: object
              properties:
                text:
                  title: text
                  description: type = PAYLOAD_TYPE_TEXT
                  $ref: '#/components/schemas/chat.v1.TextContent'
              title: text
              required:
                - text
            - type: object
              properties:
                vote:
                  title: vote
                  description: type = PAYLOAD_TYPE_VOTE
                  $ref: '#/components/schemas/chat.v1.VoteCast'
              title: vote
              required:
                - vote
            - type: object
              properties:
                voteTally:
                  title: vote_tally
                  description: type = PAYLOAD_TYPE_VOTE_TALLY
                  $ref: '#/components/schemas/chat.v1.VoteTally'
              title: vote_tally
              required:
                - voteTally
      title: Payload
      additionalProperties: false
    chat.v1.PayloadType:
      type: string
      title: PayloadType
      enum:
        - PAYLOAD_TYPE_UNSPECIFIED
        - PAYLOAD_TYPE_ROOM_CHANGE
        - PAYLOAD_TYPE_TEXT
        - PAYLOAD_TYPE_ATTACHMENT
        - PAYLOAD_TYPE_REACTION
        - PAYLOAD_TYPE_ENCRYPTED
        - PAYLOAD_TYPE_CALL
        - PAYLOAD_TYPE_MODERATION
        - PAYLOAD_TYPE_MOTION
        - PAYLOAD_TYPE_VOTE
        - PAYLOAD_TYPE_MOTION_TALLY
        - PAYLOAD_TYPE_VOTE_TALLY
      description: Allowed message types. Extendable via new enum values; clients must ignore unknown values.
    chat.v1.PresenceEvent:
      type: object
      properties:
        source:
          title: source
          $ref: '#/components/schemas/common.v1.ContactLink'
        status:
          title: status
          $ref: '#/components/schemas/chat.v1.PresenceStatus'
        statusMsg:
          type: string
          title: status_msg
        lastActive:
          title: last_active
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: PresenceEvent
      additionalProperties: false
      description: Presence event affecting a user (and visible to rooms the user is a member of)
    chat.v1.PresenceStatus:
      type: string
      title: PresenceStatus
      enum:
        - PRESENCE_STATUS_UNSPECIFIED
        - PRESENCE_STATUS_OFFLINE
        - PRESENCE_STATUS_ONLINE
    chat.v1.Proposal:
      type: object
      properties:
        id:
          type: string
          title: id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        type:
          title: type
          $ref: '#/components/schemas/chat.v1.ProposalType'
        state:
          title: state
          $ref: '#/components/schemas/chat.v1.ProposalState'
        requestedBy:
          type: string
          title: requested_by
          description: profile_id of the requester
        payload:
          title: payload
          description: the proposed change payload
          $ref: '#/components/schemas/google.protobuf.Struct'
        createdAt:
          title: created_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        expiresAt:
          title: expires_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        resolvedBy:
          type:
            - string
            - "null"
          title: resolved_by
          description: profile_id of approver/rejector
        resolvedAt:
          oneOf:
            - $ref: '#/components/schemas/google.protobuf.Timestamp'
            - type: "null"
          title: resolved_at
        reason:
          type:
            - string
            - "null"
          title: reason
          description: rejection reason if rejected
      title: Proposal
      additionalProperties: false
      description: Proposal represents a pending change that requires approval before execution.
    chat.v1.ProposalAction:
      type: string
      title: ProposalAction
      enum:
        - PROPOSAL_ACTION_UNSPECIFIED
        - PROPOSAL_ACTION_APPROVE
        - PROPOSAL_ACTION_REJECT
      description: Action to take on a proposal
    chat.v1.ProposalState:
      type: string
      title: ProposalState
      enum:
        - PROPOSAL_STATE_UNSPECIFIED
        - PROPOSAL_STATE_PENDING
        - PROPOSAL_STATE_APPROVED
        - PROPOSAL_STATE_REJECTED
        - PROPOSAL_STATE_EXPIRED
      description: ProposalState represents the current state of a proposal.
    chat.v1.ProposalType:
      type: string
      title: ProposalType
      enum:
        - PROPOSAL_TYPE_UNSPECIFIED
        - PROPOSAL_TYPE_UPDATE_ROOM
        - PROPOSAL_TYPE_DELETE_ROOM
        - PROPOSAL_TYPE_ADD_SUBSCRIPTIONS
        - PROPOSAL_TYPE_REMOVE_SUBSCRIPTIONS
        - PROPOSAL_TYPE_UPDATE_SUBSCRIPTION_ROLE
      description: ProposalType represents the kind of change being proposed.
    chat.v1.ReactionContent:
      type: object
      properties:
        targetEventId:
          type: string
          title: target_event_id
          minLength: 3
          description: Target message being reacted to
        reaction:
          type: string
          title: reaction
          maxLength: 64
          minLength: 1
          description: "Reaction key (e.g. \U0001F44D, ❤️, :custom_emoji:)"
        add:
          type: boolean
          title: add
          description: |-
            add=true → ensure reaction exists
             add=false → ensure reaction does not exist
      title: ReactionContent
      additionalProperties: false
    chat.v1.ReadMarker:
      type: object
      properties:
        roomId:
          type:
            - string
            - "null"
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        upToEventId:
          type: string
          title: up_to_event_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        subscriptionId:
          type: string
          title: subscription_id
      title: ReadMarker
      additionalProperties: false
    chat.v1.ReceiptEvent:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        eventId:
          type: array
          items:
            type: string
            maxLength: 40
            minLength: 3
            pattern: '[0-9a-z_-]{3,40}'
          title: event_id
          minItems: 1
        subscriptionId:
          type: string
          title: subscription_id
      title: ReceiptEvent
      additionalProperties: false
      description: |-
        ReceiptEvent is OPTIONAL and ephemeral.
         Servers MAY ignore it.
         Clients SHOULD prefer ReadMarker for durable state.
         Servers MUST NOT persist ReceiptEvent.
         Servers MAY drop ReceiptEvent without acknowledgment.
    chat.v1.RemoveRoomSubscriptionsRequest:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        subscriptionId:
          type: array
          items:
            type: string
          title: subscription_id
      title: RemoveRoomSubscriptionsRequest
      additionalProperties: false
    chat.v1.RemoveRoomSubscriptionsResponse:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        error:
          title: error
          $ref: '#/components/schemas/common.v1.ErrorDetail'
      title: RemoveRoomSubscriptionsResponse
      additionalProperties: false
    chat.v1.Room:
      type: object
      properties:
        id:
          type: string
          title: id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        name:
          type: string
          title: name
          maxLength: 200
          minLength: 2
        description:
          type: string
          title: description
        type:
          title: type
          $ref: '#/components/schemas/chat.v1.RoomType'
        isPrivate:
          type: boolean
          title: is_private
        metadata:
          title: metadata
          $ref: '#/components/schemas/google.protobuf.Struct'
        createdAt:
          title: created_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        updatedAt:
          title: updated_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        creatorId:
          type: string
          title: creator_id
        requiresApproval:
          type: boolean
          title: requires_approval
          description: If true, changes to this room require approval from owners before execution
      title: Room
      additionalProperties: false
    chat.v1.RoomChangeAction:
      type: string
      title: RoomChangeAction
      enum:
        - ROOM_CHANGE_ACTION_UNSPECIFIED
        - ROOM_CHANGE_ACTION_CREATED
        - ROOM_CHANGE_ACTION_UPDATED
        - ROOM_CHANGE_ACTION_DELETED
        - ROOM_CHANGE_ACTION_MEMBER_ADDED
        - ROOM_CHANGE_ACTION_MEMBER_REMOVED
        - ROOM_CHANGE_ACTION_ROLE_CHANGED
    chat.v1.RoomChangeContent:
      type: object
      properties:
        action:
          title: action
          $ref: '#/components/schemas/chat.v1.RoomChangeAction'
        actorSubscriptionId:
          type: string
          title: actor_subscription_id
        targetSubscriptionIds:
          type: array
          items:
            type: string
          title: target_subscription_ids
        body:
          type: string
          title: body
          description: Human-readable summary (server-generated, for display)
        details:
          title: details
          description: Optional structured detail (depends on action)
          $ref: '#/components/schemas/google.protobuf.Struct'
      title: RoomChangeContent
      additionalProperties: false
    chat.v1.RoomEvent:
      type: object
      properties:
        id:
          type: string
          title: id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
          description: |-
            NOTE:
             Event IDs are XIDs and MUST be lexicographically sortable by creation time.
             Clients MUST NOT assume any other ordering mechanism. client-supplied or server-generated UUID
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
          description: room_id
        subscriptionId:
          type: string
          title: subscription_id
          description: profile_id or service/bot id
        type:
          title: type
          $ref: '#/components/schemas/chat.v1.RoomEventType'
        sentAt:
          title: sent_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        edited:
          type: boolean
          title: edited
        redacted:
          type: boolean
          title: redacted
        parentId:
          type:
            - string
            - "null"
          title: parent_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
          description: if this message is a followup event on an original message
        payload:
          title: payload
          $ref: '#/components/schemas/chat.v1.Payload'
      title: RoomEvent
      additionalProperties: false
      description: Unified message (user / bot / system / signalling)
    chat.v1.RoomEventType:
      type: string
      title: RoomEventType
      enum:
        - ROOM_EVENT_TYPE_UNSPECIFIED
        - ROOM_EVENT_TYPE_SYSTEM
        - ROOM_EVENT_TYPE_EVENT
        - ROOM_EVENT_TYPE_MESSAGE
        - ROOM_EVENT_TYPE_REACTION
        - ROOM_EVENT_TYPE_TYPING
        - ROOM_EVENT_TYPE_DELIVERED
        - ROOM_EVENT_TYPE_READ
        - ROOM_EVENT_TYPE_MOTION
        - ROOM_EVENT_TYPE_CALL
        - ROOM_EVENT_TYPE_ADVERT
      description: Allowed message types. Extendable via new enum values; clients must ignore unknown values.
    chat.v1.RoomSubscription:
      type: object
      properties:
        id:
          type: string
          title: id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
          description: |-
            NOTE:
             Event IDs are XIDs and MUST be lexicographically sortable by creation time.
             Clients MUST NOT assume any other ordering mechanism.
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        member:
          title: member
          $ref: '#/components/schemas/common.v1.ContactLink'
        roles:
          type: array
          items:
            type: string
          title: roles
          description: e.g. ["owner","moderator","member","guest"]
        joinedAt:
          title: joined_at
          $ref: '#/components/schemas/google.protobuf.Timestamp'
        lastActive:
          title: last_active
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: RoomSubscription
      additionalProperties: false
    chat.v1.RoomType:
      type: string
      title: RoomType
      enum:
        - ROOM_TYPE_UNSPECIFIED
        - ROOM_TYPE_DIRECT
        - ROOM_TYPE_GROUP
        - ROOM_TYPE_CHANNEL
        - ROOM_TYPE_BOT
      description: RoomType distinguishes between different room semantics.
    chat.v1.SearchRoomSubscriptionsRequest:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        cursor:
          title: cursor
          $ref: '#/components/schemas/common.v1.PageCursor'
      title: SearchRoomSubscriptionsRequest
      additionalProperties: false
    chat.v1.SearchRoomSubscriptionsResponse:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        members:
          type: array
          items:
            $ref: '#/components/schemas/chat.v1.RoomSubscription'
          title: members
        nextCursor:
          type: string
          title: next_cursor
        prevCursor:
          type: string
          title: prev_cursor
      title: SearchRoomSubscriptionsResponse
      additionalProperties: false
    chat.v1.SearchRoomsRequest:
      type: object
      properties:
        query:
          type: string
          title: query
        properties:
          type: array
          items:
            type: string
          title: properties
        extras:
          title: extras
          $ref: '#/components/schemas/google.protobuf.Struct'
        cursor:
          title: cursor
          $ref: '#/components/schemas/common.v1.PageCursor'
      title: SearchRoomsRequest
      additionalProperties: false
    chat.v1.SearchRoomsResponse:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/chat.v1.Room'
          title: data
        nextCursor:
          type: string
          title: next_cursor
        prevCursor:
          type: string
          title: prev_cursor
      title: SearchRoomsResponse
      additionalProperties: false
    chat.v1.SendEventRequest:
      type: object
      properties:
        event:
          type: array
          items:
            $ref: '#/components/schemas/chat.v1.RoomEvent'
          title: event
          description: message payload (server will assign final event_id if empty)
      title: SendEventRequest
      additionalProperties: false
    chat.v1.SendEventResponse:
      type: object
      properties:
        ack:
          type: array
          items:
            $ref: '#/components/schemas/chat.v1.AckEvent'
          title: ack
      title: SendEventResponse
      additionalProperties: false
    chat.v1.SubmitProposalRequest:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        proposalId:
          type: string
          title: proposal_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        action:
          title: action
          $ref: '#/components/schemas/chat.v1.ProposalAction'
        reason:
          type:
            - string
            - "null"
          title: reason
          description: optional reason (typically for rejection)
      title: SubmitProposalRequest
      additionalProperties: false
    chat.v1.SubmitProposalResponse:
      type: object
      properties:
        proposal:
          title: proposal
          $ref: '#/components/schemas/chat.v1.Proposal'
        error:
          title: error
          $ref: '#/components/schemas/common.v1.ErrorDetail'
      title: SubmitProposalResponse
      additionalProperties: false
    chat.v1.SubscriptionSettings:
      type: object
      properties:
        subscriptionId:
          type: string
          title: subscription_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        notificationLevel:
          title: notification_level
          $ref: '#/components/schemas/chat.v1.NotificationLevel'
        muted:
          type: boolean
          title: muted
        archived:
          type: boolean
          title: archived
        pinned:
          type: boolean
          title: pinned
      title: SubscriptionSettings
      additionalProperties: false
      description: SubscriptionSettings holds per-room preferences for a member.
    chat.v1.TextAnnotation:
      type: object
      properties:
        type:
          title: type
          $ref: '#/components/schemas/chat.v1.TextAnnotation.Type'
        offset:
          type: integer
          title: offset
          format: int32
          description: UTF-16 offset & length for cross-platform compatibility
        length:
          type: integer
          title: length
          format: int32
        value:
          type: string
          title: value
          description: Target identifier (e.g., profile_id, room_id, URL)
      title: TextAnnotation
      additionalProperties: false
    chat.v1.TextAnnotation.Type:
      type: string
      title: Type
      enum:
        - TYPE_UNSPECIFIED
        - TYPE_MENTION_USER
        - TYPE_MENTION_ROOM
        - TYPE_LINK
        - TYPE_EMOJI
        - TYPE_HASHTAG
    chat.v1.TextContent:
      type: object
      properties:
        body:
          type: string
          title: body
          maxLength: 10000
          minLength: 1
          description: Required human-readable message body
        format:
          type: string
          title: format
          maxLength: 32
          minLength: 1
          description: |-
            Content format identifier (NOT MIME type)
             Examples: "plain", "markdown", "html-lite"
        annotations:
          type: array
          items:
            $ref: '#/components/schemas/chat.v1.TextAnnotation'
          title: annotations
          description: Structured annotations for clients (mentions, links, emojis)
        lang:
          type:
            - string
            - "null"
          title: lang
          description: Optional language hint (BCP-47), e.g. "en", "fr-CA"
      title: TextContent
      additionalProperties: false
    chat.v1.TypingEvent:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        subscriptionId:
          type: string
          title: subscription_id
        typing:
          type: boolean
          title: typing
        since:
          title: since
          $ref: '#/components/schemas/google.protobuf.Timestamp'
      title: TypingEvent
      additionalProperties: false
      description: Typing indicator
    chat.v1.UpdateRoomRequest:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        name:
          type: string
          title: name
        description:
          type: string
          title: description
        metadata:
          title: metadata
          $ref: '#/components/schemas/google.protobuf.Struct'
      title: UpdateRoomRequest
      additionalProperties: false
    chat.v1.UpdateRoomResponse:
      type: object
      properties:
        room:
          title: room
          $ref: '#/components/schemas/chat.v1.Room'
        error:
          title: error
          $ref: '#/components/schemas/common.v1.ErrorDetail'
      title: UpdateRoomResponse
      additionalProperties: false
    chat.v1.UpdateSubscriptionRoleRequest:
      type: object
      properties:
        subscriptionId:
          type: string
          title: subscription_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        roles:
          type: array
          items:
            type: string
          title: roles
      title: UpdateSubscriptionRoleRequest
      additionalProperties: false
    chat.v1.UpdateSubscriptionRoleResponse:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        error:
          title: error
          $ref: '#/components/schemas/common.v1.ErrorDetail'
      title: UpdateSubscriptionRoleResponse
      additionalProperties: false
    chat.v1.UpdateSubscriptionSettingsRequest:
      type: object
      properties:
        roomId:
          type: string
          title: room_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        subscriptionId:
          type: string
          title: subscription_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
        notificationLevel:
          oneOf:
            - $ref: '#/components/schemas/chat.v1.NotificationLevel'
            - type: "null"
          title: notification_level
        muted:
          type:
            - boolean
            - "null"
          title: muted
        archived:
          type:
            - boolean
            - "null"
          title: archived
        pinned:
          type:
            - boolean
            - "null"
          title: pinned
      title: UpdateSubscriptionSettingsRequest
      additionalProperties: false
    chat.v1.UpdateSubscriptionSettingsResponse:
      type: object
      properties:
        settings:
          title: settings
          $ref: '#/components/schemas/chat.v1.SubscriptionSettings'
        error:
          title: error
          $ref: '#/components/schemas/common.v1.ErrorDetail'
      title: UpdateSubscriptionSettingsResponse
      additionalProperties: false
    chat.v1.VoteCast:
      type: object
      properties:
        motionId:
          type: string
          title: motion_id
        choiceId:
          type: string
          title: choice_id
          description: Selected choice
      title: VoteCast
      additionalProperties: false
      description: |-
        total_votes_cast = sum(choice counts excluding invalid if configured)
            remaining_votes  = eligible_votes - total_votes_cast
            dead_votes       = tally(choice_id == "invalid")
            target_votes     = derived from PassingRule
            passed           = tally(passing_choice_id) >= target_votes
    chat.v1.VoteChoice:
      type: object
      properties:
        id:
          type: string
          title: id
        name:
          type: string
          title: name
        description:
          type:
            - string
            - "null"
          title: description
      title: VoteChoice
      additionalProperties: false
    chat.v1.VoteTally:
      type: object
      properties:
        choiceId:
          type: string
          title: choice_id
        count:
          type: integer
          title: count
      title: VoteTally
      additionalProperties: false
    common.v1.ContactLink:
      type: object
      properties:
        profileName:
          type: string
          title: profile_name
          maxLength: 250
          minLength: 3
          pattern: '[0-9a-z_-]{3,250}'
          description: Display name of the profile
        profileType:
          type: string
          title: profile_type
          maxLength: 250
          minLength: 3
          pattern: '[0-9a-z_-]{3,250}'
          description: Type of profile (e.g., "user", "organization", "service")
        profileId:
          type: string
          title: profile_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
          description: Unique identifier of the profile
        profileImageId:
          type: string
          title: profile_image_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
          description: Identifier of the profile's image/avatar
        contactId:
          type: string
          title: contact_id
          maxLength: 40
          minLength: 3
          pattern: '[0-9a-z_-]{3,40}'
          description: Unique identifier of the contact
        detail:
          type: string
          title: detail
          maxLength: 250
          minLength: 3
          pattern: '[0-9a-z_-]{3,250}'
          description: Additional contact details (e.g., email, phone, handle)
        extras:
          title: extras
          description: Extended metadata for the contact link
          $ref: '#/components/schemas/google.protobuf.Struct'
      title: ContactLink
      additionalProperties: false
      description: |-
        ContactLink represents a link between a contact and a profile in the system.
         Used for associating external contacts with internal profiles across services.
         This enables unified identity management and contact resolution.
    common.v1.ErrorDetail:
      type: object
      properties:
        code:
          type: integer
          title: code
          format: int32
          description: application code (see doc)
        message:
          type: string
          title: message
          description: human readable
        meta:
          type: object
          title: meta
          additionalProperties:
            type: string
            title: value
          description: additional machine-readable data
      title: ErrorDetail
      additionalProperties: false
      description: |-
        Standard error codes used by API responses.
         Use gRPC status codes; the application-level ErrorDetail below may carry more.
    common.v1.ErrorDetail.MetaEntry:
      type: object
      properties:
        key:
          type: string
          title: key
        value:
          type: string
          title: value
      title: MetaEntry
      additionalProperties: false
    common.v1.PageCursor:
      type: object
      properties:
        limit:
          type: integer
          title: limit
          format: int32
          description: Number of items per cursor (limit)
        page:
          type: string
          title: page
          description: Page number/ cursor representing last id from result
      title: PageCursor
      additionalProperties: false
      description: |-
        PageCursor provides standard offset-based pagination/cursor stream parameters.
         Used for list operations that return large result sets.
    compression:
      title: compression
      enum:
        - identity
        - gzip
        - br
      description: Which compression algorithm to use for this request
    connect:
      title: connect
      enum:
        - v1
      description: Define the version of the Connect protocol
    connect-protocol-version:
      type: number
      title: Connect-Protocol-Version
      enum:
        - 1
      description: Define the version of the Connect protocol
      const: 1
    connect-timeout-header:
      type: number
      title: Connect-Timeout-Ms
      description: Define the timeout, in ms
    connect.error:
      type: object
      properties:
        code:
          type: string
          examples:
            - not_found
          enum:
            - canceled
            - unknown
            - invalid_argument
            - deadline_exceeded
            - not_found
            - already_exists
            - permission_denied
            - resource_exhausted
            - failed_precondition
            - aborted
            - out_of_range
            - unimplemented
            - internal
            - unavailable
            - data_loss
            - unauthenticated
          description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
        message:
          type: string
          description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
        details:
          type: array
          items:
            $ref: '#/components/schemas/connect.error_details.Any'
          description: A list of messages that carry the error details. There is no limit on the number of messages.
      title: Connect Error
      additionalProperties: true
      description: 'Error type returned by Connect: https://connectrpc.com/docs/go/errors/#http-representation'
    connect.error_details.Any:
      type: object
      properties:
        type:
          type: string
          description: 'A URL that acts as a globally unique identifier for the type of the serialized message. For example: `type.googleapis.com/google.rpc.ErrorInfo`. This is used to determine the schema of the data in the `value` field and is the discriminator for the `debug` field.'
        value:
          type: string
          format: binary
          description: The Protobuf message, serialized as bytes and base64-encoded. The specific message type is identified by the `type` field.
        debug:
          oneOf:
            - type: object
              title: Any
              additionalProperties: true
              description: Detailed error information.
          discriminator:
            propertyName: type
          title: Debug
          description: Deserialized error detail payload. The 'type' field indicates the schema. This field is for easier debugging and should not be relied upon for application logic.
      additionalProperties: true
      description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message, with an additional debug field for ConnectRPC error details.
    encoding:
      title: encoding
      enum:
        - proto
        - json
      description: Define which encoding or 'Message-Codec' to use
    google.protobuf.ListValue:
      type: object
      properties:
        values:
          type: array
          items:
            $ref: '#/components/schemas/google.protobuf.Value'
          title: values
          description: Repeated field of dynamically typed values.
      title: ListValue
      additionalProperties: false
      description: |-
        `ListValue` is a wrapper around a repeated field of values.

         The JSON representation for `ListValue` is JSON array.
    google.protobuf.NullValue:
      type: string
      title: NullValue
      enum:
        - NULL_VALUE
      description: |-
        `NullValue` is a singleton enumeration to represent the null value for the
         `Value` type union.

         The JSON representation for `NullValue` is JSON `null`.
    google.protobuf.Struct:
      type: object
      additionalProperties:
        $ref: '#/components/schemas/google.protobuf.Value'
      description: |-
        `Struct` represents a structured data value, consisting of fields
         which map to dynamically typed values. In some languages, `Struct`
         might be supported by a native representation. For example, in
         scripting languages like JS a struct is represented as an
         object. The details of that representation are described together
         with the proto support for the language.

         The JSON representation for `Struct` is JSON object.
    google.protobuf.Struct.FieldsEntry:
      type: object
      properties:
        key:
          type: string
          title: key
        value:
          title: value
          $ref: '#/components/schemas/google.protobuf.Value'
      title: FieldsEntry
      additionalProperties: false
    google.protobuf.Timestamp:
      type: string
      examples:
        - "2023-01-15T01:30:15.01Z"
        - "2024-12-25T12:00:00Z"
      format: date-time
      description: |-
        A Timestamp represents a point in time independent of any time zone or local
         calendar, encoded as a count of seconds and fractions of seconds at
         nanosecond resolution. The count is relative to an epoch at UTC midnight on
         January 1, 1970, in the proleptic Gregorian calendar which extends the
         Gregorian calendar backwards to year one.

         All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
         second table is needed for interpretation, using a [24-hour linear
         smear](https://developers.google.com/time/smear).

         The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
         restricting to that range, we ensure that we can convert to and from [RFC
         3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

         # Examples

         Example 1: Compute Timestamp from POSIX `time()`.

             Timestamp timestamp;
             timestamp.set_seconds(time(NULL));
             timestamp.set_nanos(0);

         Example 2: Compute Timestamp from POSIX `gettimeofday()`.

             struct timeval tv;
             gettimeofday(&tv, NULL);

             Timestamp timestamp;
             timestamp.set_seconds(tv.tv_sec);
             timestamp.set_nanos(tv.tv_usec * 1000);

         Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

             FILETIME ft;
             GetSystemTimeAsFileTime(&ft);
             UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

             // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
             // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
             Timestamp timestamp;
             timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
             timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

         Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

             long millis = System.currentTimeMillis();

             Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
                 .setNanos((int) ((millis % 1000) * 1000000)).build();

         Example 5: Compute Timestamp from Java `Instant.now()`.

             Instant now = Instant.now();

             Timestamp timestamp =
                 Timestamp.newBuilder().setSeconds(now.getEpochSecond())
                     .setNanos(now.getNano()).build();

         Example 6: Compute Timestamp from current time in Python.

             timestamp = Timestamp()
             timestamp.GetCurrentTime()

         # JSON Mapping

         In JSON format, the Timestamp type is encoded as a string in the
         [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
         format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
         where {year} is always expressed using four digits while {month}, {day},
         {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
         seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
         are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
         is required. A proto3 JSON serializer should always use UTC (as indicated by
         "Z") when printing the Timestamp type and a proto3 JSON parser should be
         able to accept both UTC and other timezones (as indicated by an offset).

         For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
         01:30 UTC on January 15, 2017.

         In JavaScript, one can convert a Date object to this format using the
         standard
         [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
         method. In Python, a standard `datetime.datetime` object can be converted
         to this format using
         [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
         the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
         the Joda Time's [`ISODateTimeFormat.dateTime()`](
         http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
         ) to obtain a formatter capable of generating timestamps in this format.
    google.protobuf.Value:
      oneOf:
        - type: "null"
        - type: number
        - type: string
        - type: boolean
        - type: array
        - type: object
          additionalProperties: true
      description: |-
        `Value` represents a dynamically typed value which can be either
         null, a number, a string, a boolean, a recursive struct value, or a
         list of values. A producer of value is expected to set one of these
         variants. Absence of any variant indicates an error.

         The JSON representation for `Value` is JSON value.
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
security: []
tags:
  - name: chat.v1.ChatService
