name: Auto-Merge Labeled PRs
on:
  pull_request:
    types: [labeled, synchronize, reopened]
  check_suite:
    types: [completed]
jobs:
  auto-merge:
    uses: antinvestor/common/.github/workflows/auto-merge-labeled.yml@main
