name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  ci:
    uses: antinvestor/common/.github/workflows/service-ci.yaml@main
    with:
      app_dirs: "apps/default apps/gateway"
