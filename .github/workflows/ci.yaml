name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  test:
    uses: antinvestor/common/.github/workflows/go-test.yml@main
  lint:
    uses: antinvestor/common/.github/workflows/golangci-lint.yml@main
